# 변경 기록

이 파일은 버전마다 무엇이 바뀌었는지 기록합니다.
새 버전을 낼 때 맨 위에 추가해 주세요.

---

## v1.1.16 — 듀얼 모니터에서 모서리 피크가 옆 모니터로 열리던 문제 수정 (2026-08-02)

**듀얼 모니터를 쓰시는 분이 신고해 주신 문제를 고쳤습니다.** 모서리 피크는
창을 화면 가장자리 너머로 밀어내서 살짝만 보이게 숨기는 방식으로
동작하는데, 지금까지는 그 "화면 밖"이 정말 빈 공간(모니터가 없는 곳)이라고만
가정했습니다. 그런데 모니터가 두 대 이상이고 마침 그 방향에 다른 모니터가
바로 붙어 있으면, "화면 밖"이 실제로는 그 옆 모니터의 화면이라서 숨겨지는
대신 옆 모니터에 그대로 나타나 버렸습니다.

이제 피크를 켤 때마다 그 방향에 실제로 다른 모니터가 있는지 확인해서,
있으면 그 모니터를 침범하지 않는 만큼만(또는 아예 밀지 않고) 배치합니다.
그쪽에 모니터가 없는 진짜 화면 밖 방향은 예전처럼 그대로 숨겨집니다.
다만 옆에 모니터가 바로 붙어 있는 방향으로는 "완전히 숨기기"가 원래
불가능한 상황이라(밀어내는 순간 그 모니터에 보이게 되므로), 그 방향은
가장자리에 붙어만 있고 살짝 튀어나온 손잡이처럼 보이지는 않을 수 있습니다.

- 🐛 듀얼(멀티) 모니터에서 모서리 피크를 켜면 숨겨지지 않고 옆 모니터에
      그대로 열리던 문제 수정

## v1.1.15 — 웹 버전 캐시 갱신 (2026-08-02)

**웹 버전(오프라인 지원용 서비스 워커)의 캐시 이름을 올렸습니다.** 브라우저는
새 버전이 배포돼도 이전에 저장해 둔 파일을 계속 쓰려는 경우가 있는데,
캐시 이름을 바꾸면 이전 캐시를 정리하고 새 파일을 확실히 받아오게
됩니다. 홈페이지(hyunmilab.com)에 새로 통합된 웹 버전(`/memo`)을 포함해,
웹으로 접속하는 모든 곳에 적용됩니다.

- 🔧 서비스 워커 캐시 이름 갱신(v2 → v3) — 웹 버전 방문 시 최신 파일을
      더 확실히 받아오도록 함

## v1.1.14 — 작업표시줄 빠른 깜박임 되돌림 (2026-08-02)

**v1.1.13에서 추가한 "작업표시줄 더 빠르게 깜박이기"가 오히려 아예 안 깜박이는
문제를 일으켜 되돌렸습니다.** Windows는 앱이 작업표시줄 아이콘의 깜박이는
속도 자체를 정하도록 허용하지 않습니다. 이걸 흉내 내려고 짧은 간격으로
껐다 켰다를 반복했는데, 그렇게 하면 운영체제가 실제로 깜박임을 화면에
그리기도 전에 계속 취소·재시작을 반복하게 되어, 오히려 전혀 깜박이지
않게 되어 버렸습니다. 원래대로(속도는 Windows 기본값) 되돌려서, 느리더라도
확실히 깜박이도록 했습니다. "더 빠르게"는 Windows가 막고 있어 지원이
어렵다는 점 양해 부탁드립니다.

- 🐛 작업표시줄 깜박임이 아예 안 보이던 문제 수정 — v1.1.13 이전처럼
      확실하게 깜박이도록 되돌림 (다만 속도는 다시 Windows 기본값)

## v1.1.13 — 모서리 피크 자유 배치 + 새 메모 입력 문제 원인 재진단 + 알림 강화 (2026-08-02)

**모서리 피크로 숨긴 팝업 메모들이 겹쳐 보이던 문제를 다른 방식으로 고쳤습니다.**
v1.1.12에서는 프로그램이 순서를 정해 자동으로 줄 세웠는데, 이렇게 해도
여전히 1/3 정도씩 겹쳐 보인다는 신고를 받았습니다. 이번에는 아예 자동
배치를 강제하지 않고, 접힌 손잡이를 사용자가 원하는 세로 위치로 직접
끌어다 놓을 수 있게 바꿨습니다. 한 번 옮긴 자리는 기억해 뒀다가 다음에
다시 열어도 그 자리에서 시작합니다 (처음 켤 때만 참고용으로 자동 배치됩니다).

**"새 메모" 입력 문제, 원인을 다시 짚어봤습니다.** 이 문제는 v1.1.10, v1.1.12에서
두 차례 고쳤다고 말씀드렸지만 두 번 다 실제로는 재발했습니다. 이번에 주신
"다른 팝업을 열었다 닫거나, 창을 최소화했다 복원하면 커서가 생긴다"는
단서 덕분에 원인을 다시 짚어볼 수 있었습니다 — 이건 화면(리액트) 쪽 타이밍
문제가 아니라, Windows가 어느 창에 실제 키보드 입력을 보낼지 결정하는
좀 더 근본적인 단계에서 어긋나는 문제로 보입니다. 최소화했다 복원하는
것과 같은 효과를 내도록, 메인 창 오른쪽 스크래치패드에서 "새 메모"를
누르면 창에 강제로 포커스를 다시 씌우는 처리를 추가했습니다.
다만 이 문제는 이미 두 번의 수정이 실제로는 충분하지 않았던 이력이 있어,
이번에도 확실히 고쳐졌다고 장담하기보다는 설치해서 다시 확인해 주시면
좋겠습니다. 이번에도 재발하면 알려주세요 — 재발 시점의 구체적인 상황을
계속 알려주신 덕분에 조금씩 원인에 가까워지고 있습니다.

**알림(리마인더)이 더 눈에 띄게 바뀌었습니다.** 팝업 메모에 걸어 둔 알림
시각이 되면, 그 메모를 팝업으로 열어 둔 창도 메인 창처럼 전체가 주황색으로
바뀌며 표시됩니다 (클릭하면 확인 처리). 작업표시줄 깜박임도 더 빠른
간격으로 깜박이도록 바꿨습니다 (Windows가 깜박이는 속도 자체를 세밀하게
정할 수 있게 허용하지 않아서, 프로그램이 빠르게 켰다 껐다 반복하는
방식으로 흉내 낸 것이라 실제 Windows 알림창의 깜박임과 완전히 똑같지는
않을 수 있습니다).

- ✨ 모서리 피크로 접은 팝업 메모를 원하는 세로 위치로 자유롭게 드래그
      가능 — 옮긴 자리를 기억해 다음에도 그 자리에서 시작
- 🐛 메인 창 오른쪽 스크래치패드 "새 메모" 입력 문제, 다른 원인(Windows
      포커스 단계)으로 재진단해 다시 수정 — 이전 두 번의 수정이 불완전했던
      점 참고 부탁드립니다
- ✨ 알림 시각이 되면 그 메모를 열어 둔 팝업 창도 주황색으로 표시 (클릭 시
      확인 처리)
- 🔧 작업표시줄 깜박임 간격을 더 빠르게 조정 (근사치이며 정확한 속도
      지정은 Windows 제약상 어렵습니다)

## v1.1.12 — 모서리 피크 여러 개 겹침 수정 + 새 메모 입력 문제 완전 수정 (2026-08-02)

**모서리 피크를 여러 창에 켜면 서로 겹쳐 보이던 문제를 고쳤습니다.** 같은
가장자리에 팝업 메모 여러 개를 숨기면, 창마다 순서대로 "줄"(슬롯)을
배정해 위에서부터 하나씩 따로따로 쌓아 보여줍니다. 창을 닫거나 피크를
끄면 남은 창들이 자동으로 한 칸씩 당겨져 빈틈이 남지 않습니다.

**스크래치패드 "새 메모" 버튼을 눌러도 여전히 바로 입력이 안 된다는
신고가 다시 들어왔습니다.** v1.1.10에서 DOM을 직접 비우고 포커스를 주는
방식으로 고쳤다고 생각했는데, 실제로는 완전히 없어지지 않았던 것 같습니다.
이번에는 방식을 바꿔서, 입력창을 "비웠다가 채우는" 대신 아예 새 입력창으로
통째로 바꿔치기하도록 했습니다. 이러면 "지우는 중"이라는 애매한 중간 상태
자체가 사라져서, 어떤 타이밍에 타이핑을 시작해도 항상 새 입력창에만
글자가 들어갑니다. 헤드리스 환경에서 지연 없이 5번 반복해 확인했을 때도
모두 정상 동작해, 이전보다 훨씬 안정적일 것으로 봅니다.

- 🐛 모서리 피크를 같은 가장자리에 여러 개 켜면 서로 겹쳐 보이던 문제 수정
      — 이제 순서대로 줄 세워 각각 따로 보임
- 🐛 스크래치패드 "새 메모" 버튼을 누른 직후 입력이 씹히던 문제를 더 근본
      적인 방식(입력창 통째로 새로 만들기)으로 다시 수정

## v1.1.11 — 팝업 메모 "모서리 피크" 기능 추가 (2026-07-31)

팝업(스티커) 메모 창을 화면 가장자리에 살짝만 보이게 숨겨 두었다가, 마우스를
올리면 전체가 펼쳐지는 "모서리 피크" 기능을 추가했습니다. 항상 켜져 있는
게 아니라 각 팝업 창의 "더보기" 메뉴에서 원할 때만 켜고 끌 수 있습니다.

- ✨ 팝업 메모·"새 메모" 팝업 모두 "더보기" 메뉴에 "왼쪽/오른쪽 가장자리에
      숨기기" 항목 추가 — 켜면 창이 화면 가장자리로 밀려나 살짝만 보이다가,
      마우스를 올리면 즉시 펼쳐지고 벗어나면 잠시 후 다시 접힙니다
- 🔧 켜는 동시에 "항상 위"도 함께 켜집니다 (다른 창에 가려지면 가장자리에
      살짝 보이는 부분조차 안 보이게 되어 의미가 없기 때문)
- 🔧 저장된 메모의 팝업은 이 설정이 메모에 함께 저장되어, 다음에 다시 열어도
      접힌 상태로 시작합니다

## v1.1.10 — 스크래치패드 "새 메모" 버튼 누른 직후 입력 안 되던 문제 수정 (2026-07-31)

v1.1.9에서 새로 추가한 "새 메모" 버튼을 눌러 내용을 비운 직후 바로 타이핑을
시작하면 입력이 안 되고, 잠깐 기다렸다 치면 되던 문제를 고쳤습니다.

원인은 두 가지가 겹쳐 있었습니다. 하나는 본문 편집 영역이 화면(store 값)을
따라가는 방식이라, "새 메모"를 눌러도 실제 화면(DOM)이 비워지는 건 리액트가
그 다음 화면을 다시 그릴 때인데, 그 아주 짧은 틈에 바로 타이핑을 시작하면
글자가 "아직 안 비워진" 옛 내용 위에 먼저 들어갔다가 뒤늦게 화면이 비워지며
같이 지워져 버렸습니다. 이제 버튼을 누르는 즉시 화면도 바로 비웁니다.
다른 하나는, 확인 창(정말 지울지 묻는 창)이 뜬 뒤에는 Windows에서 창이 키보드
입력을 실제로 다시 받아들이기까지 아주 살짝 시간이 걸릴 수 있어서, 그 순간도
함께 보완했습니다.

- 🐛 스크래치패드 "새 메모" 버튼을 누르자마자 타이핑하면 입력이 안 되던 문제
      수정 — 이제 버튼을 누르면 바로 입력할 수 있습니다

## v1.1.9 — 그리기 후 내용 편집 안 되던 문제 + 드래그 실시간 미리보기 진짜 수정 + 색상 커서 + 스크래치패드 새 메모 버튼 (2026-07-31)

**그리기·주석에서 텍스트/이모지를 찍은 뒤 "내용 편집"으로 돌아가면 본문을
전혀 편집할 수 없던 문제를 고쳤습니다.** 원인은 텍스트·이모지 입력 패널이
"넣기"나 "취소"를 누르지 않고 다른 모드로 바로 넘어가도 화면에 그대로
남아 있던 것이었습니다. 이 패널은 화면에 고정 위치로 떠서 클릭을 받는
패널이라, 남아있는 동안 그 자리를 클릭하면 아래 본문이 아니라 이 보이지
않는(또는 눈에 띄지 않는) 패널이 클릭을 가로채 버렸습니다. 이제 그리기
모드를 벗어나면 열려 있던 입력 패널도 함께 자동으로 닫힙니다.

**v1.1.7에서 "고쳤다"고 했던 드래그 실시간 미리보기가 실제로는 완전히
고쳐지지 않았었습니다.** 화면을 다시 그리는 코드가 캔버스를 다시 그리라는
신호를 "값"이 아니라 "값을 바꾸는 함수(변하지 않는 참조)"로 알아채려고
해서, 정확히는 이 메모에 저장된 필기가 하나도 없는 첫 번째 획을 그릴 때만
우연히 정상 동작하고, 두 번째 획부터는(이미 저장된 필기가 있으면) 드래그
중에는 화면이 전혀 갱신되지 않고 마우스를 놓아야만 나타나는 상태였습니다.
지난 테스트에서는 매번 새 메모의 "첫 번째 획"만 확인해서 이 문제를
놓쳤습니다. 이번에 진짜 원인을 찾아 모든 획에서 드래그 중 실시간으로
보이도록 고쳤습니다.

- 🐛 그리기·주석에서 텍스트·이모지를 찍은 뒤 "내용 편집"으로 돌아가면
      본문 편집이 전혀 안 되던 문제 수정 (열려 있던 입력 패널이 남아
      클릭을 가로채던 것이 원인)
- 🐛 펜·연필·형광펜 드래그 중 실시간 미리보기가 첫 번째 획에서만 되고
      두 번째 획부터는 다시 마우스를 놓아야만 보이던 문제 완전히 수정
- ✨ 펜·연필·형광펜을 고르면 마우스 커서가 선택된 색·굵기의 작은 원으로
      바뀌어서, 드래그를 시작하기 전부터 어떤 색으로 그려질지 바로 보임
- ✨ 메인 화면 오른쪽 스크래치패드(기본 메모창)에도 "새 메모" 버튼 추가 —
      쓰다가 틀려서 하나하나 지우기보다, 눌러서 완전히 빈 상태로 바로
      다시 시작할 수 있음 (지울 내용이 있으면 한 번 확인)

## v1.1.8 — 메인 프로세스 에러 창이 다시는 안 뜨도록 안전장치 추가 (2026-07-31)

v1.1.7에서 "이미 실행 중일 때 재실행" 경로의 globalShortcut 레이스는
고쳤는데, 완전히 종료된 상태에서 아이콘을 (더블)클릭해 처음 실행할
때도 같은 문구의 에러 창이 또 뜬다는 신고가 들어왔습니다. Windows
탐색기가 더블클릭 시 프로세스를 아주 짧은 간격을 두고 두 번 띄우는
경우가 있어, 정확히 어떤 순간의 타이밍 때문인지 이 환경에서는 완전히
재현하지 못했습니다. 근본 원인을 못 박기보다, 메인 프로세스에서 어떤
예외가 나더라도 사용자에게 "A JavaScript Error occurred..." 같은
낯설고 무서운 에러 창이 다시는 뜨지 않도록 안전장치를 추가했습니다.

- 🔧 메인 프로세스에 전역 예외 처리기를 추가 — 예기치 못한 오류가 나도
      Electron 기본 에러 팝업 대신 콘솔 로그로만 남기고 앱은 계속 동작
- 🔧 빠른 캡처(Ctrl+Shift+N) 단축키 등록 부분을 try/catch로 감싸,
      혹시 앱이 준비되기 전 같은 예외적인 타이밍에 호출돼도 조용히
      실패하도록 함

## v1.1.7 — 더블클릭 JS 에러 수정 + 그리기 라이브 미리보기 + 이모지 선택창 안 보이던 문제 수정 (2026-07-31)

이미 실행 중인 상태(트레이에 숨어 있는 상태 포함)에서 아이콘을 다시
더블클릭하면 "A JavaScript Error occurred in the main process" 에러
창이 뜨던 문제를 고쳤습니다.

원인은 두 번째로 실행된 인스턴스가 락(lock)을 못 받아 곧바로
app.quit()을 부르는데도, 그보다 먼저 등록해 둔 app.whenReady().then(...)
콜백이 종료 중인 그 프로세스에서 뒤늦게 실행되면서 "앱이 완전히
준비되기 전에 전역 단축키(Ctrl+Shift+N 빠른 캡처)를 등록하려 했다"는
에러로 이어졌던 것입니다. 인스턴스 락 확인을 app.whenReady() 등록보다
먼저 하도록 순서를 바꿔, 두 번째 인스턴스에서는 아예 그 콜백이
등록되지 않게 했습니다.

그리기 화면에서 텍스트·이모지 도구로 글자를 넣는 창을 열고 그 안에서
이모지/특수문자/특수 이모티콘 선택기를 열면 아이콘들이 하나도 안 보이던
문제도 고쳤습니다. 원인은 CSS 겹침 구조상 선택기 패널이 부모 창의 좌표
기준 밖(화면 밖)으로 밀려나 그려지고 있었던 것이었는데, 이제 선택기
패널을 화면 최상단(document body)에 직접 띄우도록 고쳐서 항상 제자리에
보이게 했습니다.

또한 펜·연필·형광펜으로 그릴 때 마우스를 누른 채 움직이는 동안에는
선이 안 보이다가 마우스를 놓는 순간에야 한꺼번에 나타나던 문제도
고쳤습니다. 그리는 도중에도 화면이 매 순간 바로바로 갱신되도록 렌더링
타이밍을 조정해서, 이제 그리는 즉시 선이 실시간으로 보입니다.

- 🐛 이미 실행 중일 때 아이콘을 더블클릭하면(트레이에서 창만 다시 열려야
      하는 정상 상황) JS 에러 창이 뜨던 문제 수정
- 🐛 그리기의 텍스트·이모지 입력창에서 이모지/특수문자/특수 이모티콘
      선택기를 열면 아이콘이 안 보이던 문제 수정 (선택기 패널이 화면에
      바로 뜨도록 구조 변경)
- 🐛 펜·연필·형광펜으로 그릴 때 마우스를 놓아야만 선이 보이던 문제 수정 —
      이제 그리는 즉시 실시간으로 선이 보임

## v1.1.6 — 그림·주석에 글자·이모지·특수문자 찍기 기능 추가 (2026-07-31)

'그리기·주석' 모드에서 이미지를 깔고 그 위에 펜으로만 그릴 수 있었는데,
이제 텍스트 도구로 원하는 위치를 클릭하면 글자를 직접 타이핑하거나
기존 이모지·특수문자·특수 이모티콘 선택기에서 골라 그림 위에 찍어 넣을
수 있습니다.

- ✨ '그리기·주석' 도구 막대에 '텍스트·이모지' 도구 추가 — 캔버스를 클릭하면
      뜨는 입력창에 직접 타이핑하거나, 이모지/특수문자/특수 이모티콘
      선택기에서 골라 커서 위치에 끼워 넣은 뒤 그림 위에 찍을 수 있음
- 🐛 형광펜(과 펜·연필)으로 짧게 톡 찍기만 하면, 누르고 있는 동안엔 점이
      보였다가 손을 떼는 순간 "실수 클릭"으로 간주돼 사라지던 문제 수정 —
      이제 짧게 찍어도 그대로 저장됩니다. 특히 형광펜은 선 끝 모양이 butt라
      이 순간에만 아예 안 보이기까지 해서 "투명도가 안 보인다"처럼
      느껴졌는데, 점 하나만 찍힌 순간에도 즉시 색이 채워지도록 함께 고침
- ✨ '그리기 전체 삭제' 버튼 추가 — 필기와 배경 이미지를 한 번에 모두
      지웁니다(기존 '필기 전체 지우기'는 배경 이미지는 남겨 두고 싶을 때를
      위해 그대로 둠)

## v1.1.5 — "새 메모" 팝업(저장 전 초안)에 건 알림이 안 울리던 문제 추가 수정 (2026-07-31)

v1.1.4에서 창 간 데이터 동기화는 고쳤지만, 알림 스케줄러가 애초에 저장된
메모만 확인하고 **아직 프로젝트에 저장하지 않은 스크래치 초안**(메인
스크래치패드·'새 메모' 팝업)은 전혀 들여다보지 않았습니다. '새 메모'
팝업으로 메모를 쓰면서 저장 전에 알림을 걸어 두는 경우가 실제로는 가장
흔한 사용 패턴인데, 이 경로는 통째로 빠져 있었던 것입니다. 게다가
스크래치 초안은 메모와 별도의 저장소를 쓰는데 이 저장소에는 창 간
동기화 자체가 없어서, 팝업에서 알림을 걸어도 메인 창이 그 사실 자체를
몰랐습니다.

- 🐛 알림 스케줄러가 저장된 메모뿐 아니라 스크래치 초안(메인 스크래치패드·
      '새 메모' 팝업)의 알림도 확인하도록 수정
- 🐛 스크래치 초안 저장소에도 v1.1.4와 동일한 창 간 동기화 적용
- 🔧 알림 카드에 제목이 없으면(팝업엔 제목 입력창이 없음) 저장된 메모처럼
      본문 첫 줄을 대신 보여주고, "저장 전 메모"라고 표시. 눌러서 확인하면
      그 스크래치 초안을 바로 열 수 있음

## v1.1.4 — 팝업 메모에서 등록한 알림이 안 울리던 문제 수정 (2026-07-31)

알림 스케줄러는 메인 창 하나에서만 도는데, 팝업 메모 창에서 알림을 등록하면
그 팝업은 별도 프로세스라 메인 창이 새로 등록된 알림을 전혀 모르는 상태였고,
그래서 시각이 되어도 아무 반응이 없었습니다(창마다 데이터가 각자 따로 떠
있는 상태로, 파일(file://)로 여는 Windows 앱 환경에서는 브라우저의
storage 이벤트도 믿을 수 없었습니다).

메모/디자인 등 저장 데이터가 바뀔 때마다 Electron IPC로 다른 모든 창에
직접 알리고, 그 창들이 즉시 최신 데이터를 다시 읽어오도록 고쳤습니다(창을
많이 열어 둬도 성능에 무리 없도록 신호는 묶어서 보냅니다). 이제 팝업에서
등록한 알림도 메인 창이 곧바로 알아채고 정상적으로 울립니다.

- 🐛 팝업(스크래치 팝업 포함) 메모에서 등록한 알림이 전혀 울리지 않던 문제 수정

## v1.1.3 — Editor에 "새 메모" 버튼 추가 (2026-07-31)

프로젝트 메모를 열어 둔 채로 새 메모를 쓰려고 하면, 빈 화면인 줄 알고 그냥
입력하다가 사실은 열려 있던 그 메모를 덮어써 버리기 쉬웠습니다(자동 저장이라
바로 반영됨). Editor 헤더에 "＋ 새 메모" 버튼을 추가해, 지금 메모는 그대로
두고 오른쪽을 빈 스크래치패드로 바로 전환할 수 있게 했습니다.

- ✨ Editor(오른쪽 메모 화면) 헤더에 "새 메모" 버튼 추가 — 누르면 지금 열려 있는
      메모는 그대로 두고 빈 스크래치패드로 전환됩니다

## v1.1.2 — 팝업 메모 이모지/특수문자 패널이 창 밖으로 잘리는 문제 수정 (2026-07-30)

팝업 메모는 아주 작은 크기로도 띄울 수 있는데, 서식 툴바의 특수문자·이모지·
특수 이모티콘 패널은 항상 고정된 너비로 열려서 팝업 창보다 크면 창 밖으로
잘려 보이는 문제가 있었습니다. 이제 패널이 열릴 때 실제 창 크기를 확인해서
위/아래·좌/우로 자동으로 붙는 위치를 잡고, 창보다 크면 창 안에 들어오도록
크기(너비/높이)를 줄이고 스크롤되도록 고쳤습니다. 메인 화면의 다른 팝오버
(라벨·색상·글자 크기 등)에도 같은 방식이 적용되어 더 안정적으로 동작합니다.

- 🐛 팝업 메모에서 이모지/특수문자/특수 이모티콘 패널이 창 밖으로 잘려 나오던 문제 수정
- 🔧 팝업 메모 상단의 제목 입력창 제거 (좁은 창에서 버튼과 겹치기 쉬워 정리)
- 🔔 알림(리마인더)이 울릴 때, 메인 창을 X로 닫아 트레이에 숨겨 둔 상태였다면
      작업표시줄 아이콘 자체가 없어 깜박임이 보이지 않고 알림 카드도 확인할 수
      없던 문제 수정 — 이제 하던 작업을 방해하지 않도록 포커스는 유지한 채
      창과 작업표시줄 아이콘만 다시 보이게 한 뒤 깜박여서, 눌러서 확인할 수 있음
- 🎨 Windows 설치 파일/창/트레이 아이콘이 예전 보라색 아이콘 그대로 남아 있던
      문제 수정 — public/icon.svg(브랜드 심볼)에서 다시 뽑아 지금의 주황색
      체크리스트 아이콘으로 통일 (npm run build:win-icon 스크립트 추가)
- 🔧 메인 화면 오른쪽 스크래치패드에서 쓴 글을 프로젝트에 저장하면, 방금 저장한
      메모가 자동으로 열리지 않고 오른쪽이 다시 빈 스크래치패드로 돌아오도록 수정
      (바로 다음 메모를 쓸 수 있음). 저장된 메모는 왼쪽 목록에서 직접 눌러야
      오른쪽에 열립니다.

## v1.1.1 — 팝업 메모 조작 버그 수정 (2026-07-30)

팝업(스티커) 메모 창의 상단 바가 통째로 "창 드래그 영역"으로 잡혀 있어,
그 위에 있는 라벨·프로젝트·항상 위·⋯더보기·닫기(X) 버튼이 눌러도
반응하지 않고 창만 움직이던 문제를 고쳤습니다. 버튼을 눌렀을 때만
클릭이 되고, 나머지 영역을 눌렀을 때만 창이 움직이도록 분리했습니다.

- 🐛 팝업/스크래치 팝업 창의 X(닫기) 버튼이 눌리지 않던 문제 수정
- 🐛 팝업/스크래치 팝업 창의 ⋯더보기 버튼 및 그 메뉴(프로젝트 이동·저장·삭제 등)가
      눌리지 않던 문제 수정
- ✨ 팝업/스크래치 팝업 창에서도 사이드바 메모 카드처럼 **우클릭(컨텍스트 메뉴)** 으로
      같은 관리 메뉴(라벨·프로젝트 이동 또는 저장·삭제 등)를 바로 열 수 있도록 통일

## v1.0.0 — 첫 정식 출시 (2026-07-29)

Windows 스티커 메모를 대체하는 것을 목표로 한 첫 정식 버전입니다.

### 메모 기본

- 메모 작성, **자동 저장** (저장 버튼 없음)
- 프로젝트(폴더)로 분류, 태그, 즐겨찾기, 고정
- 실시간 검색 (제목·내용·태그)
- 컬러 라벨 6종 (긴급/오늘/진행 중/완료/업무/개인)
- 체크리스트 (완료 시 취소선 + 진행률 표시)
- 서식 (굵게·기울임·밑줄·취소선·색상·정렬·목록·인용·코드)
- 특수문자, 이모지 (검색·최근 사용·즐겨찾기)
- 템플릿 6종 (할 일, 회의록, 오늘의 기록, 아이디어, 장보기)
- 일정 알림 (매일·매주·매월 반복)
- 이전 내용 복원 (편집이 멈출 때마다 자동 스냅샷)

### 팝업 메모

- 독립된 창으로 띄우기, 여러 개 동시 사용
- 항상 위, 편집 잠금, 투명도 조절, 위치·크기 기억
- **Compact Mode** — 제목과 체크리스트만 보이는 작은 모드
- 기본 노출은 고정·그리기·더보기 3개로 최소화

### 꾸미기

- 배경색 8종 + 사용자 지정 색상
- 스킨 6종 (기본·줄노트·모눈·점선·포스트잇·종이결)
- 폰트 8종, 글자 크기 8단계
- 테두리 5종, 그림자 4단계
- 앱 강조색 사용자 지정
- 라이트 / 다크 모드 (차콜 계열, 순수 검정 미사용)

### 그리기

- 펜·연필·형광펜·지우개
- 직선·화살표·사각형·동그라미
- 이미지 위 주석
- 실행 취소 / 다시 실행, 필기 숨기기

### 복사 · 내보내기

- 내용 복사, 서식 포함 복사, 이미지로 복사
- PNG / JPG 저장, PDF 저장(인쇄 창)
- 메모 복제 (디자인·필기까지 함께)

### Windows 전용

- 설치 프로그램 (관리자 권한 불필요)
- 트레이 아이콘, 시작 프로그램 등록
- **빠른 캡처** `Ctrl + Shift + N` — 어디서든 새 팝업 메모
- Windows 알림 센터 연동

### 웹 버전

- PWA 지원, 오프라인 사용, 반응형

### 데이터 안전

- 메모 내용 / 디자인 / 필기를 **서로 다른 저장소에 분리 보관**
  → 꾸미기·그리기 기능을 고쳐도 글이 손상되지 않습니다
- 예전 버전 데이터 자동 변환, 변환 전 원본 보존
- 백업 가져오기는 기본이 **합치기** (기존 메모 삭제 안 함)
- 프로젝트를 지워도 안에 있던 메모는 '미분류'로 이동만

---

## 앞으로의 계획

| 버전 | 목표 | 내용 |
|---|---|---|
| **v1.0.1** | 버그 수정 | 출시 후 보고된 문제 해결. 기능 추가 없음 |
| **v1.1** | 사용성 개선 | 단축키 확장, 검색 고도화, 목록 정렬 옵션, 접근성 |
| **v1.2** | 꾸미기 개선 | 스킨·테마 추가, 핀보드, 스마트 스탬프 |
| **v2.0** | AI 기능 | 요약·자동 분류·문장 다듬기 (중계 서버 필요) |

### 버전 규칙

```
v1.0.1  →  버그 수정만. 데이터 구조 변경 없음
v1.1.0  →  기능 추가. 기존 데이터 그대로 열림
v2.0.0  →  큰 변화. 데이터 구조가 바뀌면 자동 변환 제공
```

### 새 버전을 낼 때

1. `package.json` 의 `version` 수정
2. 이 파일 맨 위에 변경 내용 추가
3. `website/index.html` 아래쪽 다운로드 링크와 푸터 버전 표기 수정
4. 아래 순서로 빌드

```bash
npm run build:win     # 설치 파일
npm run shots         # 스크린샷 다시 촬영 (화면이 바뀌었다면)
npm run build:site    # 배포용 사이트 폴더
```

> **데이터 구조를 바꿀 때는** `src/types/index.ts` 의 `SCHEMA_VERSION` 을 올리고
> `src/lib/storage.ts` 에 변환 코드를 추가하세요.
> 기존 메모가 손상되지 않는 것이 이 앱의 가장 중요한 원칙입니다.
