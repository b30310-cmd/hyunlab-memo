# HYUNLAB Memo 디자인 시스템

화면마다 버튼 크기나 여백이 달라 보이지 않도록, 값을 **한 곳에서만** 정합니다.
새 화면을 만들 때는 아래 규칙만 따르면 자동으로 통일감이 유지됩니다.

---

## 1. 값은 어디에 있나

| 파일 | 담는 것 |
|---|---|
| `src/index.css` | 색·간격·둥글기·높이·속도 (CSS 변수) |
| `tailwind.config.js` | 위 변수를 Tailwind 클래스로 연결 |
| `src/components/ui/Button.tsx` | 버튼·아이콘버튼·메뉴항목 + 아이콘 크기 |

**직접 색상 코드(`#333`)나 임의 크기(`size={11}`)를 쓰지 마세요.**
반드시 아래 토큰을 쓰면 라이트/다크 전환과 통일성이 자동으로 따라옵니다.

---

## 2. 색

```
--bg          앱 바탕
--surface     카드·패널·툴바
--surface-2   입력창 등 한 단계 안쪽
--border      경계선
--text        본문 글자
--text-muted  보조 글자
--text-faint  날짜·개수 등 가장 옅은 글자
--accent      강조색 (설정에서 사용자가 변경)
```

Tailwind에서는 `bg-surface`, `text-muted`, `border-line`, `text-accent` 처럼 씁니다.

### 다크 모드
순수 검정(`#000`)은 쓰지 않습니다. 푸른기가 도는 **차콜** 계열입니다.

| | 라이트 | 다크 |
|---|---|---|
| 바탕 | `#f7f8fa` | `#16181d` |
| 표면 | `#ffffff` | `#1c1f26` |
| 본문 글자 | `#1a1c20` | `#e8eaed` |

**글자 대비비는 가장 옅은 단계까지 4.5:1 이상**을 지킵니다. (WCAG AA)

| | 라이트 | 다크 |
|---|---|---|
| 본문 | 16.1:1 | 14.7:1 |
| 보조 | 7.1:1 | 8.3:1 |
| 옅음 | 4.55:1 | 4.96:1 |

---

## 3. 크기

```
높이   --h-sm 28px   --h-md 32px   --h-lg 36px
둥글기 --r-sm 8px    --r-md 10px   --r-lg 14px   --r-xl 18px
간격   4 / 8 / 12 / 16 / 20 / 24 (4의 배수만)
```

### 아이콘 (Lucide 한 종류만 사용)

```ts
ICON.xs = 12  // 카드 안 상태 표시(고정·즐겨찾기)
ICON.sm = 14  // 칩, 좁은 툴바
ICON.md = 16  // 일반 버튼, 메뉴
ICON.lg = 18  // 상단 툴바, 헤더
```

이 4단계 외의 숫자는 쓰지 않습니다.

### 글자 크기 (6단계)

```
xs 11px   sm 12px   base 13px   md 14px   lg 16px   xl 19px
```

메모 제목만 예외로 22px(굵게)을 써서 가장 먼저 눈에 들어오게 했습니다.

---

## 4. 컴포넌트

새 버튼을 만들 때는 직접 스타일을 쓰지 말고 아래를 가져다 씁니다.

```tsx
import { Button, IconButton, MenuItem, MenuDivider, MenuLabel, ICON } from '@/components/ui/Button'

<Button variant="primary">저장</Button>      // 주요 동작
<Button>취소</Button>                         // 보조 (기본값 secondary)
<Button variant="ghost">더보기</Button>       // 배경 없음
<Button variant="danger">삭제</Button>

<IconButton title="설정" onClick={...}>       // 항상 정사각형
  <Settings size={ICON.lg} />
</IconButton>

<MenuItem icon={<Copy size={ICON.md} />}>복제</MenuItem>
```

`title`은 접근성상 필수입니다 (`aria-label`로도 함께 들어갑니다).

---

## 5. 동작(애니메이션)

```
--t-fast 120ms   hover, 색 변화
--t-base 180ms   메뉴/팝업 열림
```

- 메뉴·팝오버: `animate-pop` (살짝 떠오르며 나타남)
- 모달 배경: `animate-fade`
- 버튼 클릭: `scale(0.97)` — 눌리는 느낌만
- `prefers-reduced-motion`을 켠 사용자에게는 모든 애니메이션이 꺼집니다.

과한 효과는 넣지 않습니다. 빠른 반응이 우선입니다.

---

## 6. 화면 구성 원칙

**기능은 많아도 첫 화면은 단순해야 합니다.**

| 노출 단계 | 내용 |
|---|---|
| 항상 보임 | 새 메모, 검색, 제목, 본문, 고정/즐겨찾기/팝업 |
| 칩을 눌러야 | 서식, 꾸미기, 그리기, 알림, 태그 |
| ⋯ 메뉴 안 | 복사, 내보내기, 프로젝트 이동, 기록 복원, 삭제 |
| 우클릭 | 그 메모에 필요한 동작만 (라벨·고정·복제·이동·삭제) |

팝업 메모는 특히 엄격합니다. 기본은 **📌 고정 / ✏️ 그리기 / ⋯ 더보기 / ✕ 닫기** 네 개뿐이고,
나머지 11개 기능은 전부 더보기 안에 있습니다.

---

## 7. 컬러 라벨

메모를 한눈에 구분하는 색 점입니다. `src/lib/constants.ts`의 `LABELS`에서 관리합니다.

| 라벨 | 색 |
|---|---|
| 🔴 긴급 | `#ef4444` |
| 🟠 오늘 | `#f97316` |
| 🟡 진행 중 | `#eab308` |
| 🟢 완료 | `#22c55e` |
| 🔵 업무 | `#3b82f6` |
| 🟣 개인 | `#a855f7` |

메모 카드에서는 제목 왼쪽에 작은 점으로, 편집 화면에서는 헤더 왼쪽에 점+이름으로 표시됩니다.

---

## 8. 새 화면을 만들 때 체크리스트

- [ ] 색은 토큰(`bg-surface`, `text-muted` …)만 썼는가
- [ ] 버튼은 `Button` / `IconButton`을 썼는가
- [ ] 아이콘 크기는 `ICON.*`만 썼는가 (Lucide만 사용)
- [ ] 간격은 4의 배수인가
- [ ] 자주 안 쓰는 기능을 기본 화면에 노출하지 않았는가
- [ ] 다크 모드에서도 읽히는가
