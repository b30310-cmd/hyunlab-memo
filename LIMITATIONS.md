# 제한 사항과 대안

요구하신 기능 중 **지금 구조로는 제대로 만들 수 없는 것**을 흉내 내지 않고 그대로 적었습니다.
"되는 척"하는 기능은 넣지 않았습니다.

---

## 1. 아직 만들지 않은 기능

### 🚫 AI 기능 — 만들지 않았습니다

**왜 못 만드나**
AI 기능(요약, 자동 분류, 글 다듬기)은 반드시 **외부 AI 서버에 요청을 보내야** 합니다.
그러려면 세 가지가 필요합니다.

1. API 키 (예: Anthropic, OpenAI) — **유료**
2. 키를 숨겨 둘 **중계 서버** — 앱에 키를 넣으면 누구나 꺼내 쓸 수 있어 요금 폭탄이 납니다
3. 사용자가 쓴 메모 내용이 외부로 전송된다는 **동의 절차**

지금 이 앱은 서버가 전혀 없는 100% 오프라인 앱이라, 위 조건을 만족할 수 없습니다.

**대안**
- (권장) HYUNLAB 명의로 간단한 중계 서버를 하나 두고, 앱은 그 서버에만 요청합니다.
  서버 비용이 들지만 키가 안전하고 사용량 제어가 됩니다.
- 또는 설정에 "내 API 키 직접 입력" 칸을 만들어, 원하는 사용자만 자기 키로 쓰게 합니다.
  (서버 불필요 / 하지만 일반 사용자는 쓰기 어렵습니다)

---

### 🚫 기기 간 동기화 — 만들지 않았습니다

**왜 못 만드나**
동기화는 "파일을 복사하는 것"이 아니라 **충돌 해결**이 본체입니다.
PC와 노트북에서 같은 메모를 동시에 고치면 어느 쪽을 남길지 결정해야 하는데,
이건 서버 + 계정 + 로그인 + 충돌 병합 로직이 모두 있어야 합니다.
서버 없이 흉내만 내면 **메모가 조용히 사라지는 사고**가 반드시 납니다.
(요구사항의 "기존 메모가 손상되지 않아야 한다"와 정면으로 충돌합니다)

**대안 (지금 바로 쓸 수 있음)**
1. **수동 백업 방식** — 설정 → 내보내기로 JSON 저장 → 다른 PC에서 가져오기.
   가져오기는 기본이 **합치기**라서 기존 메모가 지워지지 않습니다.
2. **클라우드 폴더 방식** — 데이터 저장 위치를 OneDrive/구글드라이브 폴더로 바꾸면
   파일 동기화로 어느 정도 흉내가 납니다. 다만 두 PC를 동시에 켜면 충돌합니다.
   (이 방식을 원하시면 저장 위치 변경 기능을 추가해 드릴 수 있습니다)
3. 제대로 하려면 **서버 + 로그인**이 필요합니다. 별도 프로젝트 규모입니다.

---

### 🚫 핀보드 — 만들지 않았습니다

메모를 자유 위치에 붙여 두는 보드 화면입니다.
드래그 배치, 확대/축소, 겹침 순서, 위치 저장이 필요해 **4단계 그리기보다 큰 작업**입니다.
지금은 **팝업 메모**가 사실상 같은 역할(바탕화면에 자유 배치)을 하므로 우선순위를 낮췄습니다.
필요하시면 다음 단계로 만들 수 있습니다.

---

### 🚫 스마트 스탬프 — 부분만 있습니다

"스마트 스탬프"가 무엇을 뜻하는지에 따라 작업량이 크게 달라집니다.

- **날짜/시간 도장** 정도라면 → 지금도 템플릿의 `{date}`로 자동 입력됩니다.
- **상태 라벨(완료/보류/긴급) 도장**이라면 → 만들 수 있습니다. (반나절 정도)
- **손글씨 인식·자동 도형 보정**이라면 → 인식 엔진이 필요해 별도 작업입니다.

어느 쪽을 원하시는지 알려주시면 그에 맞춰 만들겠습니다.

---

## 2. 만들었지만 조건이 붙는 기능

### ⚠️ PDF 저장 — "인쇄 창"이 열립니다

브라우저에는 PDF를 조용히 만들어 저장하는 표준 기능이 없습니다.
PDF 라이브러리를 넣으면 **한글 폰트 전체를 앱에 포함**해야 해서
용량이 수십 MB 늘고 글꼴이 깨지기 쉽습니다.

그래서 메모를 이미지로 만든 뒤 **인쇄 창**을 엽니다.
사용자는 "대상 → PDF로 저장"을 고르면 됩니다. (Windows·웹 동일)

### ⚠️ 이미지로 복사 — 브라우저에 따라 다릅니다

| 환경 | 동작 |
|---|---|
| Windows 설치 버전 | ✅ 정상 |
| Chrome / Edge | ✅ 정상 |
| Firefox | ⚠️ 이미지 클립보드 미지원 → "PNG로 저장"을 쓰세요 |
| Safari | ⚠️ 제한적 |

### ⚠️ 자동 업데이트 — 코드 서명이 필요합니다

`electron-updater`를 붙이는 것 자체는 어렵지 않지만,
**코드 서명 인증서(연 10~40만원)** 와 업데이트 파일을 올려둘 서버가 필요합니다.
서명이 없으면 업데이트 때마다 Windows 경고가 떠서 오히려 불신을 줍니다.
인증서를 준비하시면 그때 붙이는 것을 권합니다.

---

## 3. 웹 버전 vs Windows 설치 버전

| 기능 | 웹 | Windows 설치 |
|---|:--:|:--:|
| 메모 작성·자동 저장 | ✅ | ✅ |
| 프로젝트·검색·태그 | ✅ | ✅ |
| 꾸미기(색/스킨/폰트/테두리) | ✅ | ✅ |
| 그리기·주석 | ✅ | ✅ |
| 복사·PNG/JPG 저장 | ✅ | ✅ |
| 팝업 메모 | △ 브라우저 팝업 창 | ✅ 진짜 독립 창 |
| **항상 위** | ❌ 불가 | ✅ |
| **창 투명도** | ❌ 불가 | ✅ |
| **창 위치 기억** | △ 부정확 | ✅ 정확 |
| **빠른 캡처(Ctrl+Shift+N)** | ❌ 불가 | ✅ |
| **트레이 아이콘** | ❌ | ✅ |
| **시작프로그램 등록** | ❌ | ✅ |
| 알림 | △ 브라우저 알림 | ✅ Windows 알림센터 |
| 오프라인 | ✅ PWA 설치 시 | ✅ |

> ❌ 표시된 것은 **브라우저 보안 정책상 웹에서 영원히 불가능**한 기능입니다.
> 흉내 낼 방법이 없으므로 웹 버전에서는 해당 버튼을 아예 감췄습니다.

---

## 4. 데이터 안전 설계 (요구사항 반영)

"기능 추가로 기존 메모가 손상되면 안 된다"를 지키기 위해 이렇게 만들었습니다.

**① 저장소를 3개로 분리했습니다**

| 저장소 | 담는 것 | 잃으면? |
|---|---|---|
| `memos` | 글 내용·제목·태그 | 치명적 |
| `designs` | 색·폰트·스킨·테두리 | 모양만 기본값으로 |
| `drawings` | 펜·형광펜 필기 | 그림만 사라짐 |

꾸미기나 그리기 코드를 고치다 문제가 생겨도 **글 내용은 건드리지 않습니다.**

**② 구조가 바뀌면 자동 변환합니다**
예전 버전 메모도 앱을 켜면 자동으로 새 구조로 옮겨집니다.
변환 전 원본은 `memos:backup-v1` 키에 **그대로 남겨 둡니다.**

**③ 지우지 않습니다**
- 프로젝트를 삭제해도 안에 있던 메모는 '미분류'로 이동할 뿐 삭제되지 않습니다.
- 백업 가져오기는 기본이 **합치기**입니다. 교체는 따로 확인을 받습니다.
- 메모 수정 시 자동으로 이전 내용 스냅샷이 쌓여 언제든 되돌릴 수 있습니다.

**④ 검증했습니다**
데이터 안전 관련 항목을 자동 테스트로 확인했습니다. (본문 17개 + 설치본 10개 통과)

---

## 5. 다음에 만들면 좋을 순서

1. **핀보드** — 팝업 메모를 한 화면에 모아 보는 보드
2. **스마트 스탬프** — 완료/긴급 등 상태 도장 (원하시는 정의 확인 필요)
3. **저장 위치 변경** — 클라우드 폴더를 이용한 간이 동기화
4. **자동 업데이트** — 코드 서명 인증서 준비 후
5. **AI 기능** — 중계 서버 준비 후
