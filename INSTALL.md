# 설치 안내

## 설치 방법

1. `HYUNLAB-Memo-Setup-1.0.0.exe` 를 실행합니다.
2. **파란 경고 화면이 나오면** 아래 "경고 화면 대처법"을 보세요.
3. 설치 폴더를 확인하고 [설치]를 누릅니다.
4. 바탕화면과 시작 메뉴에 바로가기가 만들어집니다.

관리자 권한이 필요 없습니다. 회사 PC에서도 설치됩니다.

---

## ⚠️ 경고 화면 대처법 (가장 흔한 문제)

설치를 실행하면 이런 파란 화면이 뜰 수 있습니다.

```
Windows의 PC 보호
Microsoft Defender SmartScreen에서 인식할 수 없는 앱의 시작을
차단했습니다. 이 앱을 실행하면 PC가 위험해질 수 있습니다.

                          [실행 안 함]
```

**해결 방법**

1. 왼쪽의 작은 글씨 **[추가 정보]** 를 클릭합니다.
2. 아래에 나타나는 **[실행]** 버튼을 누릅니다.

> **왜 이런 화면이 뜨나요?**
> 이 앱에 **코드 서명 인증서**가 없기 때문입니다. 바이러스라서가 아닙니다.
> Windows는 서명이 없는 새 프로그램을 전부 이렇게 막습니다.
>
> 인증서는 1년에 10~40만 원 정도의 유료 상품이라 지금은 넣지 않았습니다.
> 배포를 본격적으로 하실 계획이면 인증서를 구입해 서명하는 것을 권합니다.
> 서명을 넣으면 이 화면이 사라집니다. (자세한 내용은 `LIMITATIONS.md` 참고)

---

## 설치가 실패할 때

### 설치 폴더에 빈 폴더만 생기고 끝난 경우

`C:\Program Files\` 같은 **관리자 권한이 필요한 폴더**를 설치 위치로 고르면
파일을 쓰지 못해 설치가 중간에 멈춥니다.

**1.0.0 버전부터는 그런 폴더를 아예 고를 수 없도록 막아 두었습니다.**
(선택하면 [다음] 버튼이 비활성화됩니다)

기본 설치 위치를 그대로 두는 것을 권장합니다.

```
C:\Users\사용자이름\AppData\Local\Programs\HYUNLAB Memo
```

만약 예전에 실패해서 `C:\Program Files\HYUNLAB Memo` 라는 빈 폴더가 남아 있다면,
그 폴더는 지워도 됩니다. (지울 때 관리자 권한을 물어봅니다)

### 백신 프로그램이 막는 경우

일부 백신은 서명 없는 설치 파일을 격리합니다.
백신의 격리 목록에서 복원하거나, 예외로 등록한 뒤 다시 실행해 주세요.

### 설치는 됐는데 실행이 안 되는 경우

작업 관리자에서 `HYUNLAB Memo.exe` 가 이미 실행 중인지 확인해 주세요.
이 앱은 **하나만 실행되도록** 만들어져 있어서, 이미 켜져 있으면
새로 실행해도 기존 창이 앞으로 나올 뿐 새 창이 뜨지 않습니다.
트레이(시계 옆 아이콘)에 숨어 있을 수도 있습니다.

---

## 제거 방법

`설정 → 앱 → 설치된 앱`에서 **HYUNLAB Memo**를 제거합니다.

**메모는 지워지지 않습니다.** 프로그램만 제거되고 작성한 메모는 그대로 남아,
다시 설치하면 이어서 쓸 수 있습니다.

메모까지 완전히 지우려면 제거 후 아래 폴더를 삭제하세요.

```
C:\Users\사용자이름\AppData\Roaming\HYUNLAB Memo
```

> 메모를 옮기거나 백업하려면 앱에서 **설정 → 백업 → 내보내기**를 사용하세요.

---

## 웹 버전

설치 없이 바로 쓰고 싶다면 웹 버전을 이용하세요.
다만 항상 위 고정, 창 투명도, 빠른 캡처(Ctrl+Shift+N), 트레이 아이콘은
브라우저 보안 정책상 **웹에서는 동작하지 않습니다.**

기능 차이는 `LIMITATIONS.md` 의 비교표를 참고하세요.
